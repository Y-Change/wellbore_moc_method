# Paper A 中文初稿交付审计

审计日期：2026-09-06

## 稿件

- 中文初稿已按摘要、引言、前向模型与观测算子、机理证伪、物理信息重建、四个极端工况验证、讨论与下游反演接口、结论组织。
- 文中 12 组显示公式和全部行内数学定界符通过配对检查；显示公式内不存在嵌套的行内定界符。
- 已知先验限定为名义射孔深度，实际进液活动 \(z_k\) 保持为非负未知量。\(K_{\mathrm{comp}}\) 使用有限物理增益，不使用局部峰值目标除法。

## 数据回查

- `decay_table.csv` 含 4320 条簇级记录。定常摩阻数据中，\(n=2\)--8 有 420 个多簇拓扑，单簇基线有 6 个独立首簇深度，共 426 个独立拓扑。
- \(X_1=3000\,\mathrm m,S=20\,\mathrm m\) 时，\(P_1\) 从 5.1573949 降至 2.7052892，降幅 47.545%。
- \(L_{\mathrm{span}}=60\,\mathrm m\) 时，\(n=2,3,4,7\) 的 \(R_{\mathrm{end}}\) 分别为 0.4722、0.1877、0.1582、0.0716。
- \(n=8,S=20\,\mathrm m\) 的峰列为 [3.420, 1.616, 0.695, 0.554, 0.406, 0.274, 0.231, 0.184]，\(R_{\mathrm{end}}=0.0537\)。

## 图件

- Figure 1--10 均有 400-dpi PNG、可编辑 SVG 和 PDF，并已在原始 PNG 分辨率下目视检查。
- Figure 3 和 Figure 4 已改为前向证据/算子模板图，不再显示目标均衡结果或 oracle 恢复率；两图的面板对齐检查均为 PASS。
- Figure 8 已从完整的 70 行 \(S\)-\(n\) 指标网格和 60 行 \(X_1\)-\(S\) 指标网格重绘为统一的六面板图。其面板对齐、PDF 字号和碰撞审计均为 PASS。
- 旧版 5×1 oracle 图和 target-equalization 校准图保留用于审计，不进入论文证据链。

## 参考文献

- 主稿含 22 条独立 DOI 记录；Crossref 回查为 22/22 成功，BibTeX 键和 DOI 均无重复；新增 5 条的题名、期刊、卷和文章号又经 ScienceDirect/MDPI 出版方页面复核。
- 正文引用按首次出现顺序覆盖 [1]--[22]，无漏号或越界引用。
- 新增 5 条 2025--2026 研究，覆盖进液深度检测、滤波敏感性、高频簇识别、监测装备和自动参数反演。正文只引用题名和元数据能够支持的研究范围，不引述未经全文核对的精度或恢复率。
- 本轮不需要论文全文或补充材料中的定量结果，因此未执行全文或补充材料批量下载。

## 结论边界

426 个拓扑验证了非局部峰值、间距谷值、等跨度失效和选定多路径深度。它们没有提供完整的部分激活真值、独立复相位校准或现场噪声重复，因而不能支持 100% 盲恢复、普适伪峰抑制或定量裂缝开度反演。Figure 3 中 \(T_{\mathrm{eff}}=0.784\) 仅为有限级联增益模板的示意值，并非由峰值表识别的参数。
