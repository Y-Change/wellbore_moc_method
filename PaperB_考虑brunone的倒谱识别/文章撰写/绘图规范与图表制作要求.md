# Paper B 论文绘图规范与图表制作标准要求

> **文档性质**：本文档为 *Paper B（Brunone 非定常摩阻下的波形退化与倒谱模糊机理研究）* 的**统一绘图技术规范与排版生产标准**。
> **目标期刊风格**：严格对标 *SPE Journal / Mechanical Systems and Signal Processing (MSSP) / Nature Portfolio* 等国际顶级期刊标准。
> **生效范围**：Paper B 论文正文所有插图（Figure 1 至 Figure 6 及附录/方法图件）。

---

## 1. 核心排版与几何比例规范

### 1.1 版面尺寸与子图比例
*   **通栏双栏宽度 (Double-column width)**：总宽度控制在 **$7.0 \sim 7.35\text{ in}$ ($180\text{ mm}$)**，高度控制在 **$2.2 \sim 2.6\text{ in}$**（单排 3 子图结构）；
*   **单栏半栏宽度 (Single-column width)**：总宽度控制在 **$3.5\text{ in}$ ($89\text{ mm}$)**；
*   **子图高宽比 (Subpanel Box Aspect Ratio)**：
    *   所有二维曲线子图的轴框比例统一设定为 **高 : 宽 = 4 : 5**（代码实现：`ax.set_box_aspect(4/5)`）；
    *   热力图/时频图子图根据数据维度精确对齐，并保持色条高度与主图一致。

### 1.2 四周全封边与刻度规范
*   **全封闭边框 (Full Box Enclosing Spines)**：
    *   开启所有四面边框（`top=True, bottom=True, left=True, right=True`）；
    *   边框线宽统一为 **$0.75\text{ pt}$**（`axes.linewidth = 0.75`），颜色为黑色（`#000000`）。
*   **刻度线朝向 (Inward Ticks)**：
    *   主刻度线朝向必须**向内**（`xtick.direction = 'in', ytick.direction = 'in'`）；
    *   开启上侧与右侧对齐刻度（`xtick.top = True, ytick.right = True`）；
    *   主刻度长度为 **$3.0\text{ pt}$**，次刻度为 **$1.5\text{ pt}$**，线宽 **$0.75\text{ pt}$**。

---

## 2. 字体与文字排版规范

### 2.1 字体家族
*   **西文与正文字体**：统一强制采用 **Times New Roman**（备用回退字体：`Times, DejaVu Serif, serif`）；
*   **数学公式与特殊符号**：采用与 Times 完美契合的 **STIX 数学字体集**（代码实现：`mpl.rcParams['mathtext.fontset'] = 'stix'`）；
*   **禁止项**：严禁在最终投稿图中混用 Arial、Calibri 或 SimHei 等无衬线/非标准中文字体。

### 2.2 字号层级标准
| 元素类型 | 标准字号 (pt) | 样式 (Style) | 格式说明 |
| :--- | :--- | :--- | :--- |
| **子图序号** | **9.5 pt** | **加粗 (Bold)** | 必须使用带小括号格式：**(a)**, **(b)**, **(c)**，置于子图左上方（`loc='left', pad=5`） |
| **坐标轴标题 (Axis Labels)** | **8.0 pt** | 常规 (Regular) | 物理量名称 + 符号 + 括号单位，如 `Time $t$ (s)`, `Quefrency $\tau$ (s)` |
| **坐标轴刻度 (Tick Labels)** | **7.0 pt** | 常规 (Regular) | 紧贴刻度线，数值间距均匀 |
| **图例文本 (Legend Text)** | **6.5 ~ 7.0 pt** | 常规 (Regular) | 无外框（`frameon=False`），控制 handlelength 为 1.0 ~ 1.2 |
| **色条标注 (Colorbar Label)** | **6.8 ~ 7.0 pt** | 常规 (Regular) | 紧贴色阶条侧边，字号与刻度匹配 |

---

## 3. 色彩与色阶布局规范

### 3.1 调色板定义 (Curated Nature/SCI Palette)
全套图件统一采用高对比度、色盲友好（Colorblind-Friendly）的高级学术色谱：

```python
PALETTE = {
    "steady_darcy": "#1B4F72",   # 深海蓝（稳态 Darcy 基准组，k=0）
    "k_001":        "#117864",   # 墨青色（弱非定常，k=0.01）
    "k_002":        "#2874A6",   # 明蓝色（k=0.02）
    "k_005":        "#D35400",   # 珊瑚橙（中度非定常，k=0.05）
    "k_010":        "#BA4A00",   # 赭石橙（k=0.10）
    "k_020":        "#900C3F",   # 勃艮第酒红（强非定常锚点，k=0.20）
    "true_pos":     "#7D3C98",   # 皇家紫（真实物理位置标记线）
    "detected_pos": "#C0392B",   # 鲜红（倒谱识别位置虚线）
    "gray_ref":     "#7F8C8D",   # 中性灰（参考线、背景网格）
}
```

### 3.2 连续色阶规范 (Continuous Colormaps)
*   **时频能量分布 (STFT Spectrogram)**：必须采用感知均匀的 **`viridis`** 或 **`magma`** 色阶，禁止使用彩虹色 `jet`；
*   **双峰对比度热力图 ($C_v$ Heatmap)**：采用 **`mako`** 或 **`Blues_r`** 单调渐变色谱，明确标注 $C_v = 0$（完全融合）与 $C_v = 1.0$（完全分离）。

---

## 4. 论文 6 幅核心主图规划与规格

| 编号 | 图件名称 | 形式 | 核心要传达的信息 |
| :--- | :--- | :--- | :--- |
| **Figure 1** | **波形时域退化演化图** | 3 子图通栏 | 展示不同 $k$ 下水头导数 $\mathrm{d}H/\mathrm{d}t$ 的峰值锐减、脉冲平滑与波包展宽过程 |
| **Figure 2** | **峰漂移与能量弥散趋势图** | 2 子图半栏/通栏 | 定量呈现 $\Delta t_{\text{peak}}$（277 ms 漂移）与 EST 随 $k$ 及间距 $D$ 的单调恶化曲线 |
| **Figure 3** | **共同参考 STFT 时频图与频带能量耗散** | 3 子图通栏 | 展示 $k=0$ vs $k=0.2$ 的 STFT 时频能量演变及三大频带的 dB 级选择性衰减（高频 $-44.1\text{ dB}$） |
| **Figure 4** | **表观波速分化与视波速偏离** | 2 子图 | 对比 $a_{f0}$（降 11.0%）、$a_{\text{onset}}$（降 1.3%）、$a_{\text{peak}}$（降 4.7%）的表观分离 |
| **Figure 5** | **倒谱峰钝化与系统性深度偏差 (10–20m)** | 3 子图通栏 | 对比稳态 vs Brunone 真实倒谱剖面、容差扫描 $F_1$ 曲线与深度偏差分布 |
| **Figure 6** | **多裂缝倒谱混叠与流变粘度敏感性** | 3 子图通栏 | 呈现密集裂缝倒谱混叠破坏效应，及滑溜水 $\to$ 线性胶 $\to$ 冻胶的倒谱逐级模糊退化图 |
