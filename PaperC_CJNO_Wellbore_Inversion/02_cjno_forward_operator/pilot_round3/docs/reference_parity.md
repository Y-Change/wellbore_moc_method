# Round-3 C：同物理参考对齐

F 离散定义：`F^{n+1} = p_in^{n+1} - p_c^{n+1} = c1 q^{n+1} + c0`，单位 Pa，在每次成功的簇 Newton 之后、新时间层上更新。  
**禁止**等分 Σq、复制平均 pc、F=0 冒充真状态。本轮孪生从同一 `solve_steady_state` 导出逐孔 `q, pc, F` 与左右迹。

长窗协议（与 B 的 8 s 分开）：`T = t_s + t_c + 2*(4L/a) + 2 x_1/a`，由案例物理量决定。

序列：无损 → 同 Darcy → 生产 ZVB。**Darcy 过门 ≠ 生产物理过门。**

## 离散孪生 vs 同网格参考 MOC

4 个 train 重放案（嵌套前缀）：`pilot_01342, 00163, 00543, 01320`。孪生 `nx=64`。

| case | T_long / s | 4L/a / s | twin vs ref none | twin vs ref Darcy | n_fail |
|---|---|---|---|---|---|
| 01342 | 25.87 | 9.89 | **0** | **0** | 0/0 |
| 00163 | 35.81 | 14.00 | **0** | **0** | 0/0 |
| 00543 | 32.73 | 12.70 | **0** | **0** | 0/0 |
| 01320 | 32.85 | 12.78 | **0** | **0** | 0/0 |

残差 inf 约 `1e-10`。方程与初态对齐后，同网格井口扰动 L2 为机器零。  
Cr<1 用与 `moc_solver` 相同的脚点插值；`dt_reduced=True` 时全局减小 dt，**不**把时延截成至少一步。

这是**同离散算子对齐**，不是学习 2% 门。

## 网格加密前置条件（新 0.5% 门的前提）

参考 `simulate` Darcy，64 vs 128，井口扰动 L2：

| case | 64 vs 128 | 是否「足够小」于 0.2% 预检 |
|---|---|---|
| 01342 | 2.49% | 否 |
| 00163 | 0.70% | 否 |
| 00543 | 0.49% | 否 |
| 01320 | 0.45% | 否 |

前置「参考加密差足够小」**未满足**。因此：**不能**把「每案 ≤0.5%」写成已对连续极限成立的新对齐门。  
同网格孪生=0 只证明 Python 孪生复现了该 Nx 的参考离散式。

## ZVB

- Tensor ZVB 记忆：**NOT_RUN**
- 参考 `zvb_rec` vs Darcy（缩短窗、nx=32，避免长窗超时）：扰动 L2 约 0.9–3.0%。两者不同。
- 长窗 ZVB 参考：第一次尝试 nx=64、T≈26 s **超时中止**，记 **TIMEOUT**，不记 FAIL。

## 稳态漂移

阀保持常开（`t_s→∞`）、无损：相对漂移 **0**，PASS。

## 因果（见 fixups）

首次 C 跑把 K 乘进井规格后**重解稳态**，井口 t=0 就不同，不能当 x/a 检验。  
修正：冻结初态后再改 K。nx=32 上首差时刻与 `x/a` 同量级，但偏差 0.19–0.58 s，**未过**「≤1.5 dt」门。记 **FAIL**（粗网格/检测阈值），不是 PASS。  
阀→节点：原记录相对 `t_s+x/a` 偏早约 0.8–1.1 s，同样不宣称过门。

JSON：`manifests/production_rollout_gates.json`，`production_rollout_fixups.json`。
